# Site of Care deck: cleanup edits

Paste this whole file to the Claude add-in in the open PowerPoint deck
("ATC vs Non-ATC Site of Care Analysis"). These are small, exact edits, not a redesign.

## How to work
- Change only what each item names. Do not restyle, recolor, move, or resize anything else.
- Keep the existing fonts, sizes, card layout, bars, and colors exactly as they are.
- Match the wording given here character for character.
- Do one slide at a time and show me the result before moving to the next.
- If a value or box is not where I describe it, stop and ask rather than guessing.

---

## Fix 1. Slide 8 (State view, non-ATC accounts): the Texas card

The Texas card right now shows one row, "Texas Oncology 211", and an italic note
underneath ("One account carries most of the state..."). Replace the whole card body
so it matches the other five cards: three account rows with the small olive bar under
each, no italic note.

Texas card, three rows (name on the left, patient count on the right):

| Account | Patients |
|---|---|
| Texas Oncology | 210 |
| Shannon Medical Center | 20 |
| Providence St. Joseph Health | 19 |

Delete the italic note line. Give each row the same small olive bar the other cards
use, on the same shared scale (the longest bar on the slide is Michigan's 531).
Texas Oncology's bar should be the longest of the three; the other two are slivers.

---

## Fix 2. Slide 3 (Market structure): trim the footnote

In the small footnote under the table, find and delete this sentence:

> ATC share (approximately 46%) remains an estimate as classification currently relies in part on account-name matching pending the updated NPI roster.

Keep the rest of the footnote (the "Corrected July 2026 ... reconciled against the
authorized ATC account list" sentence) exactly as it is. Change nothing else on the slide.

---

## Fix 3. Slide 4 (Patient journey): label the base correctly

The four boxes (99, 8,775, 7,482, 48) are measured on the treatment cohort, which is
slightly larger than the 16,246 diagnosed patients shown on slide 3. Right now each box
reads "% of all patients", which implies the 16,246 base and does not add up to it. Two
small edits fix this:

1. In all four boxes, change the caption "of all patients" to "of treated patients".
   (So "0.6% of all patients" becomes "0.6% of treated patients", and the same for the
   other three.) Do not change any number or percentage.

2. Add one sentence to the footnote at the bottom of the slide:

> The journey is measured on the treatment cohort (patients with at least one treatment claim), which is slightly larger than the 16,246 diagnosed patients shown on slide 3 because it does not require a prior diagnosis claim.

Leave the boxes, the claims strip, and everything else on the slide unchanged.

---

## Optional, only if you want it

- Slide 6 (state scatter): New York still appears as a dot, but slide 8 says New York
  drops out of the top states. If you want them to agree, remove the New York dot and its
  label from the scatter. Leave every other dot where it is. Skip this if unsure.
