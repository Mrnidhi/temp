# tableau/

Empty on purpose. `python RUN_ALL.py` writes the three `.hyper` extracts here.

They are deliberately not committed. They are regenerable in one command, and a sample-data
extract sitting in this folder could be opened in Tableau and mistaken for real data.
