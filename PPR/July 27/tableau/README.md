# tableau/

Empty on purpose. `python RUN_ALL.py` writes the three `.hyper` extracts here.

They are deliberately not committed. They are regenerable in one command, and a synthetic
extract sitting in this folder on the office laptop could be opened in Tableau and mistaken
for real data.
